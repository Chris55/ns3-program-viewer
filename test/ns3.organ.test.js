// this file is auto-generated with builder.js

const { getNs3TestCase } = require("./helpers");

const root = __dirname + "/ns3/organ/";

describe("/ns3/organ", () => {
    test("panelA.organ.enabled eq false", async () => {
        const file = "panelA.organ.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.enabled eq true", async () => {
        const file = "panelA.organ.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq ----", async () => {
        const file = "panelA.organ.kbZone.value eq ----.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq ---O", async () => {
        const file = "panelA.organ.kbZone.value eq ---O.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq --O-", async () => {
        const file = "panelA.organ.kbZone.value eq --O-.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq --OO", async () => {
        const file = "panelA.organ.kbZone.value eq --OO.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq -O--", async () => {
        const file = "panelA.organ.kbZone.value eq -O--.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq -OO-", async () => {
        const file = "panelA.organ.kbZone.value eq -OO-.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq -OOO", async () => {
        const file = "panelA.organ.kbZone.value eq -OOO.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq O---", async () => {
        const file = "panelA.organ.kbZone.value eq O---.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq OO--", async () => {
        const file = "panelA.organ.kbZone.value eq OO--.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq OOO-", async () => {
        const file = "panelA.organ.kbZone.value eq OOO-.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.kbZone.value eq OOOO", async () => {
        const file = "panelA.organ.kbZone.value eq OOOO.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.octaveShift.value eq -1", async () => {
        const file = "panelA.organ.octaveShift.value eq -1.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.octaveShift.value eq 0", async () => {
        const file = "panelA.organ.octaveShift.value eq 0.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.octaveShift.value eq 1", async () => {
        const file = "panelA.organ.octaveShift.value eq 1.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.decayFast.enabled eq false", async () => {
        const file = "panelA.organ.percussion.decayFast.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.decayFast.enabled eq true", async () => {
        const file = "panelA.organ.percussion.decayFast.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.enabled eq false", async () => {
        const file = "panelA.organ.percussion.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.enabled eq true", async () => {
        const file = "panelA.organ.percussion.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.harmonicThird.enabled eq false", async () => {
        const file = "panelA.organ.percussion.harmonicThird.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.harmonicThird.enabled eq true", async () => {
        const file = "panelA.organ.percussion.harmonicThird.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.volumeSoft.enabled eq false", async () => {
        const file = "panelA.organ.percussion.volumeSoft.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.percussion.volumeSoft.enabled eq true", async () => {
        const file = "panelA.organ.percussion.volumeSoft.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.pitchStick.enabled eq false", async () => {
        const file = "panelA.organ.pitchStick.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.pitchStick.enabled eq true", async () => {
        const file = "panelA.organ.pitchStick.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.preset1.morph.wheel.to.value eq 21-------", async () => {
        const file = "panelA.organ.preset1.morph.wheel.to.value eq 21-------.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.preset1.morph.wheel.to.value eq 864-32101", async () => {
        const file = "panelA.organ.preset1.morph.wheel.to.value eq 864-32101.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.preset1.value eq 123456780 and panelA.organ.preset1.morph.wheel.enabled eq false", async () => {
        const file = "panelA.organ.preset1.value eq 123456780 and panelA.organ.preset1.morph.wheel.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.sustainPedal.enabled eq false", async () => {
        const file = "panelA.organ.sustainPedal.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.sustainPedal.enabled eq true", async () => {
        const file = "panelA.organ.sustainPedal.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq B3 and panelA.organ.preset1.value eq 345666554 and panelA.organ.preset2.value eq 864111367", async () => {
        const file = "panelA.organ.type.value eq B3 and panelA.organ.preset1.value eq 345666554 and panelA.organ.preset2.value eq 864111367.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq B3 and panelA.organ.preset1.value eq 355788653 and panelA.organ.preset2.value eq 343323343", async () => {
        const file = "panelA.organ.type.value eq B3 and panelA.organ.preset1.value eq 355788653 and panelA.organ.preset2.value eq 343323343.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq B3 and panelA.organ.preset1.value eq 888888888 and panelA.organ.preset2.value eq 888888888", async () => {
        const file = "panelA.organ.type.value eq B3 and panelA.organ.preset1.value eq 888888888 and panelA.organ.preset2.value eq 888888888.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq Farfisa and panelA.organ.preset1.morph.wheel.to.value eq -0-11--00", async () => {
        const file = "panelA.organ.type.value eq Farfisa and panelA.organ.preset1.morph.wheel.to.value eq -0-11--00.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq Farfisa and panelA.organ.preset1.value eq 111001111 and panelA.organ.preset2.value eq 111111111", async () => {
        const file = "panelA.organ.type.value eq Farfisa and panelA.organ.preset1.value eq 111001111 and panelA.organ.preset2.value eq 111111111.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq Pipe1 and panelA.organ.preset1.value eq 887544333 and panelA.organ.preset2.value eq 888888888", async () => {
        const file = "panelA.organ.type.value eq Pipe1 and panelA.organ.preset1.value eq 887544333 and panelA.organ.preset2.value eq 888888888.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq Pipe2 and panelA.organ.preset1.value eq 008000000 and panelA.organ.preset2.value eq 888888888", async () => {
        const file = "panelA.organ.type.value eq Pipe2 and panelA.organ.preset1.value eq 008000000 and panelA.organ.preset2.value eq 888888888.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq Vox and panelA.organ.preset1.morph.wheel.to.value eq --66325-0", async () => {
        const file = "panelA.organ.type.value eq Vox and panelA.organ.preset1.morph.wheel.to.value eq --66325-0.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.type.value eq Vox and panelA.organ.preset1.value eq 888888808 and panelA.organ.preset2.value eq 888888808", async () => {
        const file = "panelA.organ.type.value eq Vox and panelA.organ.preset1.value eq 888888808 and panelA.organ.preset2.value eq 888888808.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.enabled eq false", async () => {
        const file = "panelA.organ.vibrato.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.enabled eq true", async () => {
        const file = "panelA.organ.vibrato.enabled eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.mode.value eq C1", async () => {
        const file = "panelA.organ.vibrato.mode.value eq C1.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.mode.value eq C2", async () => {
        const file = "panelA.organ.vibrato.mode.value eq C2.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.mode.value eq C3", async () => {
        const file = "panelA.organ.vibrato.mode.value eq C3.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.mode.value eq V1", async () => {
        const file = "panelA.organ.vibrato.mode.value eq V1.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.mode.value eq V2", async () => {
        const file = "panelA.organ.vibrato.mode.value eq V2.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.vibrato.mode.value eq V3", async () => {
        const file = "panelA.organ.vibrato.mode.value eq V3.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.afterTouch.enabled eq true and panelA.organ.volume.morph.afterTouch.to.value eq 0.0 dB", async () => {
        const file = "panelA.organ.volume.morph.afterTouch.enabled eq true and panelA.organ.volume.morph.afterTouch.to.value eq 0.0 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.afterTouch.enabled eq true and panelA.organ.volume.morph.afterTouch.to.value eq Off", async () => {
        const file = "panelA.organ.volume.morph.afterTouch.enabled eq true and panelA.organ.volume.morph.afterTouch.to.value eq Off.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.controlPedal.enabled eq true and panelA.organ.volume.morph.controlPedal.to.value eq 0.0 dB", async () => {
        const file = "panelA.organ.volume.morph.controlPedal.enabled eq true and panelA.organ.volume.morph.controlPedal.to.value eq 0.0 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.controlPedal.enabled eq true and panelA.organ.volume.morph.controlPedal.to.value eq Off", async () => {
        const file = "panelA.organ.volume.morph.controlPedal.enabled eq true and panelA.organ.volume.morph.controlPedal.to.value eq Off.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.wheel.enabled eq false and panelA.organ.volume.morph.afterTouch.enabled eq false and panelA.organ.volume.morph.controlPedal.enabled eq false", async () => {
        const file = "panelA.organ.volume.morph.wheel.enabled eq false and panelA.organ.volume.morph.afterTouch.enabled eq false and panelA.organ.volume.morph.controlPedal.enabled eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.wheel.enabled eq true and panelA.organ.volume.morph.wheel.to.value eq 0.0 dB", async () => {
        const file = "panelA.organ.volume.morph.wheel.enabled eq true and panelA.organ.volume.morph.wheel.to.value eq 0.0 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.morph.wheel.enabled eq true and panelA.organ.volume.morph.wheel.to.value eq Off", async () => {
        const file = "panelA.organ.volume.morph.wheel.enabled eq true and panelA.organ.volume.morph.wheel.to.value eq Off.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.value eq -0.1 dB", async () => {
        const file = "panelA.organ.volume.value eq -0.1 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.value eq -12.2 dB", async () => {
        const file = "panelA.organ.volume.value eq -12.2 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.value eq -2.0 dB", async () => {
        const file = "panelA.organ.volume.value eq -2.0 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.value eq -7.6 dB", async () => {
        const file = "panelA.organ.volume.value eq -7.6 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.value eq -7.8 dB", async () => {
        const file = "panelA.organ.volume.value eq -7.8 dB.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.organ.volume.value eq Off", async () => {
        const file = "panelA.organ.volume.value eq Off.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset1.morph.afterTouch.to.value eq 012------", async () => {
        const file = "panelB.organ.preset1.morph.afterTouch.to.value eq 012------.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset1.morph.controlPedal.to.value eq 888888888", async () => {
        const file = "panelB.organ.preset1.morph.controlPedal.to.value eq 888888888.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset1.morph.wheel.to.value eq 887-2--54", async () => {
        const file = "panelB.organ.preset1.morph.wheel.to.value eq 887-2--54.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset1.value eq 233334366 and panelB.organ.preset2.value eq 882221388", async () => {
        const file = "panelB.organ.preset1.value eq 233334366 and panelB.organ.preset2.value eq 882221388.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset2.morph.afterTouch.to.value eq ------76-", async () => {
        const file = "panelB.organ.preset2.morph.afterTouch.to.value eq ------76-.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset2.morph.controlPedal.to.value eq ---------", async () => {
        const file = "panelB.organ.preset2.morph.controlPedal.to.value eq ---------.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset2.morph.wheel.to.value eq ----64--4", async () => {
        const file = "panelB.organ.preset2.morph.wheel.to.value eq ----64--4.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelB.organ.preset2.value eq 682221388", async () => {
        const file = "panelB.organ.preset2.value eq 682221388.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

});

