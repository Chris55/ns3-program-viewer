// this file is auto-generated with test-builder.js

const { loadTestCase } = require("./helpers");

const root = __dirname + "/ns2/piano/";

describe("/ns2/piano", () => {
    test("slotA.piano.clavinetEq.value eq Medium", async () => {
        const file = "slotA.piano.clavinetEq.value eq Medium.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetEq.value eq Soft+Medium", async () => {
        const file = "slotA.piano.clavinetEq.value eq Soft+Medium.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetEq.value eq Soft", async () => {
        const file = "slotA.piano.clavinetEq.value eq Soft.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetEqHi.value eq Brilliant", async () => {
        const file = "slotA.piano.clavinetEqHi.value eq Brilliant.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetEqHi.value eq Treble+Brilliant", async () => {
        const file = "slotA.piano.clavinetEqHi.value eq Treble+Brilliant.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetEqHi.value eq Treble", async () => {
        const file = "slotA.piano.clavinetEqHi.value eq Treble.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetModel.value eq A", async () => {
        const file = "slotA.piano.clavinetModel.value eq A.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetModel.value eq B", async () => {
        const file = "slotA.piano.clavinetModel.value eq B.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetModel.value eq C", async () => {
        const file = "slotA.piano.clavinetModel.value eq C.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.clavinetModel.value eq D", async () => {
        const file = "slotA.piano.clavinetModel.value eq D.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.dynamics.value eq 0", async () => {
        const file = "slotA.piano.dynamics.value eq 0.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.dynamics.value eq 1", async () => {
        const file = "slotA.piano.dynamics.value eq 1.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.dynamics.value eq 2", async () => {
        const file = "slotA.piano.dynamics.value eq 2.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.dynamics.value eq 3", async () => {
        const file = "slotA.piano.dynamics.value eq 3.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.enabled eq false", async () => {
        const file = "slotA.piano.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.enabled eq true", async () => {
        const file = "slotA.piano.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbGate.enabled eq false", async () => {
        const file = "slotA.piano.kbGate.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbGate.enabled eq true", async () => {
        const file = "slotA.piano.kbGate.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbZone.value eq HI", async () => {
        const file = "slotA.piano.kbZone.value eq HI.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbZone.value eq LO UP HI", async () => {
        const file = "slotA.piano.kbZone.value eq LO UP HI.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbZone.value eq LO UP", async () => {
        const file = "slotA.piano.kbZone.value eq LO UP.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbZone.value eq LO", async () => {
        const file = "slotA.piano.kbZone.value eq LO.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbZone.value eq UP HI", async () => {
        const file = "slotA.piano.kbZone.value eq UP HI.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.kbZone.value eq UP", async () => {
        const file = "slotA.piano.kbZone.value eq UP.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.latchPedal.enabled eq false", async () => {
        const file = "slotA.piano.latchPedal.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.latchPedal.enabled eq true", async () => {
        const file = "slotA.piano.latchPedal.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.longRelease.enabled eq false", async () => {
        const file = "slotA.piano.longRelease.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.longRelease.enabled eq true", async () => {
        const file = "slotA.piano.longRelease.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.octaveShift.value eq -7 oct", async () => {
        const file = "slotA.piano.octaveShift.value eq -7 oct.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.octaveShift.value eq @p0 oct", async () => {
        const file = "slotA.piano.octaveShift.value eq @p0 oct.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.octaveShift.value eq @p6 oct", async () => {
        const file = "slotA.piano.octaveShift.value eq @p6 oct.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.pedalNoise.enabled eq false", async () => {
        const file = "slotA.piano.pedalNoise.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.pedalNoise.enabled eq true", async () => {
        const file = "slotA.piano.pedalNoise.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.pitchStick.enabled eq false", async () => {
        const file = "slotA.piano.pitchStick.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.pitchStick.enabled eq true", async () => {
        const file = "slotA.piano.pitchStick.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.slotDetune.value eq 1", async () => {
        const file = "slotA.piano.slotDetune.value eq 1.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.slotDetune.value eq 2", async () => {
        const file = "slotA.piano.slotDetune.value eq 2.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.slotDetune.value eq 3", async () => {
        const file = "slotA.piano.slotDetune.value eq 3.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.slotDetune.value eq 4", async () => {
        const file = "slotA.piano.slotDetune.value eq 4.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.stringResonance.enabled eq false", async () => {
        const file = "slotA.piano.stringResonance.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.stringResonance.enabled eq true", async () => {
        const file = "slotA.piano.stringResonance.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.sustainPedal.enabled eq false", async () => {
        const file = "slotA.piano.sustainPedal.enabled eq false.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.sustainPedal.enabled eq true", async () => {
        const file = "slotA.piano.sustainPedal.enabled eq true.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.midi eq 33 and slotA.piano.volume.morph.wheel.to.midi eq 66", async () => {
        const file = "slotA.piano.volume.midi eq 33 and slotA.piano.volume.morph.wheel.to.midi eq 66.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.midi eq 50 and slotA.piano.volume.morph.wheel.to.midi eq 51", async () => {
        const file = "slotA.piano.volume.midi eq 50 and slotA.piano.volume.morph.wheel.to.midi eq 51.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.midi eq 51 and slotA.piano.volume.morph.wheel.to.midi eq 50", async () => {
        const file = "slotA.piano.volume.midi eq 51 and slotA.piano.volume.morph.wheel.to.midi eq 50.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.midi eq 66 and slotA.piano.volume.morph.wheel.to.midi eq 33", async () => {
        const file = "slotA.piano.volume.midi eq 66 and slotA.piano.volume.morph.wheel.to.midi eq 33.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.value eq 0.0 dB and slotA.piano.volume.morph.wheel.to.value eq Off", async () => {
        const file = "slotA.piano.volume.value eq 0.0 dB and slotA.piano.volume.morph.wheel.to.value eq Off.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.value eq 0.0 dB", async () => {
        const file = "slotA.piano.volume.value eq 0.0 dB.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.value eq Off and slotA.piano.volume.morph.controlPedal.to.value eq 0.0 dB", async () => {
        const file = "slotA.piano.volume.value eq Off and slotA.piano.volume.morph.controlPedal.to.value eq 0.0 dB.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.value eq Off and slotA.piano.volume.morph.wheel.to.value eq 0.0 dB", async () => {
        const file = "slotA.piano.volume.value eq Off and slotA.piano.volume.morph.wheel.to.value eq 0.0 dB.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("slotA.piano.volume.value eq Off", async () => {
        const file = "slotA.piano.volume.value eq Off.ns2p";
        const sut = await loadTestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

});

