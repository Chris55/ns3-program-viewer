// this file is auto-generated with builder.js

const { getNs3TestCase } = require("./helpers");

const root = __dirname + "/ns3/Synth/envelopes/";

describe("/ns3/Synth/envelopes", () => {
    test("panelA.synth.envelopes.amplifier.attack.midi eq 0 and panelA.synth.envelopes.amplifier.attack.label eq 0.5 ms", async () => {
        const file = "panelA.synth.envelopes.amplifier.attack.midi eq 0 and panelA.synth.envelopes.amplifier.attack.label eq 0.5 ms.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.attack.midi eq 127 and panelA.synth.envelopes.amplifier.attack.label eq 45 s", async () => {
        const file = "panelA.synth.envelopes.amplifier.attack.midi eq 127 and panelA.synth.envelopes.amplifier.attack.label eq 45 s.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.decay.midi eq 0 and panelA.synth.envelopes.amplifier.decay.label eq 3.0 ms", async () => {
        const file = "panelA.synth.envelopes.amplifier.decay.midi eq 0 and panelA.synth.envelopes.amplifier.decay.label eq 3.0 ms.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.decay.midi eq 127 and panelA.synth.envelopes.amplifier.decay.label eq Sustain", async () => {
        const file = "panelA.synth.envelopes.amplifier.decay.midi eq 127 and panelA.synth.envelopes.amplifier.decay.label eq Sustain.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.release.midi eq 0 and panelA.synth.envelopes.amplifier.release.label eq 3.0 ms", async () => {
        const file = "panelA.synth.envelopes.amplifier.release.midi eq 0 and panelA.synth.envelopes.amplifier.release.label eq 3.0 ms.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.release.midi eq 127 and panelA.synth.envelopes.amplifier.release.label eq 45 s", async () => {
        const file = "panelA.synth.envelopes.amplifier.release.midi eq 127 and panelA.synth.envelopes.amplifier.release.label eq 45 s.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.velocity eq 1", async () => {
        const file = "panelA.synth.envelopes.amplifier.velocity eq 1.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.velocity eq 2", async () => {
        const file = "panelA.synth.envelopes.amplifier.velocity eq 2.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.velocity eq 3", async () => {
        const file = "panelA.synth.envelopes.amplifier.velocity eq 3.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.amplifier.velocity eq Off", async () => {
        const file = "panelA.synth.envelopes.amplifier.velocity eq Off.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.attack.midi eq 0 and panelA.synth.envelopes.modulation.attack.label eq 0.5 ms", async () => {
        const file = "panelA.synth.envelopes.modulation.attack.midi eq 0 and panelA.synth.envelopes.modulation.attack.label eq 0.5 ms.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.attack.midi eq 127 and panelA.synth.envelopes.modulation.attack.label eq 45 s", async () => {
        const file = "panelA.synth.envelopes.modulation.attack.midi eq 127 and panelA.synth.envelopes.modulation.attack.label eq 45 s.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.decay.midi eq 0 and panelA.synth.envelopes.modulation.decay.label eq 3.0 ms", async () => {
        const file = "panelA.synth.envelopes.modulation.decay.midi eq 0 and panelA.synth.envelopes.modulation.decay.label eq 3.0 ms.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.decay.midi eq 127 and panelA.synth.envelopes.modulation.decay.label eq Sustain", async () => {
        const file = "panelA.synth.envelopes.modulation.decay.midi eq 127 and panelA.synth.envelopes.modulation.decay.label eq Sustain.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.release.midi eq 0 and panelA.synth.envelopes.modulation.release.label eq 3.0 ms", async () => {
        const file = "panelA.synth.envelopes.modulation.release.midi eq 0 and panelA.synth.envelopes.modulation.release.label eq 3.0 ms.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.release.midi eq 127 and panelA.synth.envelopes.modulation.release.label eq Inf", async () => {
        const file = "panelA.synth.envelopes.modulation.release.midi eq 127 and panelA.synth.envelopes.modulation.release.label eq Inf.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.velocity eq false", async () => {
        const file = "panelA.synth.envelopes.modulation.velocity eq false.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

    test("panelA.synth.envelopes.modulation.velocity eq true", async () => {
        const file = "panelA.synth.envelopes.modulation.velocity eq true.ns3f";
        const sut = await getNs3TestCase(root + file);
        sut.data.forEach((d) => {
            expect(d.actual).toEqual(d.expected);
        });
    });

});

